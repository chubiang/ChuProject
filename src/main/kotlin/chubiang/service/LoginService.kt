package chubiang.service

import org.springframework.stereotype.Service

@Service
class LoginService {
    fun loginPage(type: String): String{
        if(type.equals("fail")) {

        }else if(type.equals(""))
    }
}